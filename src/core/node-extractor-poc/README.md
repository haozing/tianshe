# Node Extractor PoC

This placeholder is intentionally no longer a runtime entrypoint.

The v4 Site Adapter foundation lives under `src/core/site-adapter-runtime`. New read-only adapters should be created as example or site packages, not inside this PoC directory.
